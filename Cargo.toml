[workspace]
members = [
    "src/nexus_core",
    "src/nexus_gui",
    "src/nexus",
    "src/nexus_sandbox",
    "src/nexus_mcp",
    "src/nexus_recorder",
    "src/nexus_exporter",
    "src/nexus_notetaker",
    "src/nexus_storage",
    "src/x_toolbox",
]
# x_toolbox is excluded from default builds (Linux-only, requires specific DB setup)
# Build explicitly with: cargo build -p x_toolbox
default-members = [
    "src/nexus_core",
    "src/nexus_gui",
    "src/nexus",
    "src/nexus_sandbox",
    "src/nexus_mcp",
    "src/nexus_recorder",
    "src/nexus_exporter",
    "src/nexus_notetaker",
    "src/nexus_storage",
]
resolver = "2"

[workspace.dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "multipart", "rustls-tls"] }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive", "env"] }
ratatui = "0.29"
tokio-stream = "0.1"
uuid = { version = "1", features = ["v4", "serde"] }
quick-xml = { version = "0.33", features = ["serialize"] }
 
